#include <ginger/unistd.h>
#include <api.h>

void main(void) {
    // int         err     = 0;
    // pid_t       pid     = 0;
    // int         stat_loc= 0;
    // char *const  shell[] = {"/ramfs/shell", NULL};

    loop() {
        fork();
    }
}